# MOSLock V3 — Streamlit App Requirements
# Scenario 1 & 2 (Remote Demo + Tablet Demo without model):
streamlit>=1.40.0
Pillow>=11.0.0
pandas>=2.0.0

# Scenario 3 (Tablet Live — add these for ONNX inference):
onnxruntime>=1.20.0
numpy>=1.26.4

# Note: onnxruntime and numpy are commented out for Streamlit Community Cloud
# deployment (Live Mode is disabled gracefully when not installed).
# Uncomment for local tablet installation.
